DEBUG=True
SECRET_KEY=change-me
ALLOWED_HOSTS=127.0.0.1,localhost
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/sibec
# Production example (set explicit origins)
# CORS_ALLOWED_ORIGINS=https://sibec.tu-dominio.com
# Local development fallback is automatic in DEBUG when both CORS vars are empty.
# Optional regex list for advanced setups:
# CORS_ALLOWED_ORIGIN_REGEXES=^https?://localhost(:\d+)?$,^https?://127\.0\.0\.1(:\d+)?$
